VITE_PUSHER_APP_KEY=local-key
VITE_PUSHER_APP_CLUSTER=mt1
# Use Soketi directly
VITE_PUSHER_HOST=localhost
VITE_PUSHER_PORT=6001
VITE_PUSHER_SCHEME=http

# Video storage/config
# Cloudflare R2 public base URL for serving uploaded assets (e.g., thumbnails/videos)
# Example: https://<PUBLIC_BUCKET_SUBDOMAIN>.r2.cloudflarestorage.com/<bucket>
VITE_CLOUDFLARE_R2_PUBLIC_BASE_URL=

# Cloudflare API base + account for Stream/R2 (used by frontend only if needed; typically backend will call this)
# Example: https://api.cloudflare.com/client/v4/accounts/<ACCOUNT_ID>
VITE_CLOUDFLARE_API_BASE=
VITE_CLOUDFLARE_ACCOUNT_ID=

# YouTube embed base URL (used to render iframes for lessons hosted on YouTube)
# Example: https://www.youtube.com/embed/
VITE_YOUTUBE_EMBED_BASE=https://www.youtube.com/embed/

# Preferred source order when multiple URLs exist: cloudflare,youtube,local
VITE_VIDEO_SOURCE_PRIORITY=cloudflare,youtube,local

## API base and proxy target for Vite dev server
# Axios base URL used by frontend API client
VITE_API_BASE_URL=/api
# Proxy target host for Vite to forward /api requests (nginx from docker-compose)
# If running backend outside docker, set to http://localhost:8000
VITE_API_TARGET=http://localhost:8080
